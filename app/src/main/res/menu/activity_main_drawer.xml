<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">


    <group
        android:id="@+id/nav_top"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_locate"
            android:icon="@drawable/ic_menu_locate"
            android:title="@string/menu_locate" />
        <item
            android:id="@+id/nav_savedmessages"
            android:icon="@drawable/ic_savedmessages"
            android:title="@string/menu_savedmessages"/>
    </group>

    <group
        android:id="@+id/nav_bottom"
        android:checkableBehavior="single">
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings"
                android:title="@string/menu_settings" />
            <item
                android:id="@+id/nav_exit"
                android:icon="@drawable/ic_menu_exit"
                android:title="@string/menu_exit" />
    </group>

</menu>
